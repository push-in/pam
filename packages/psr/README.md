# PAM PSR compatibility package

## Start here

This package preserves existing `pushinbr/pam-psr` installations. New applications must use:

```bash
pam composer require pushinbr/pam-http-psr
```

Install PAM before using either package. This bridge exists only for migration and is abandoned in
Composer in favor of `pushinbr/pam-http-psr`.
